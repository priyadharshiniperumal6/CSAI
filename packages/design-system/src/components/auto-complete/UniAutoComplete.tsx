import { AutoComplete } from 'antd';
import type { AutoCompleteProps } from 'antd';

import './UniAutoComplete.scss';

export const UniAutoComplete = (props: AutoCompleteProps) => <AutoComplete {...props} />;
